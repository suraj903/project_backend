spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sportsgeekdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081


#db:
#  queries:
#    listOfVenue: SELECT VenueId,Name FROM Venue